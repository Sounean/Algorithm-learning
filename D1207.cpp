#include "iostream"
#include "cmath"
#include "string"
#include "algorithm"
using namespace std;

int n;
double arr[450];

int main() {

//    cin >> n;
//    for (int i = 0; i < n; i++) {
//        cin >> arr[i];
//    }
//
//    double max1 = arr[0];
//   int  maxIndex =0;
//    double min1 = arr[0];
//    for (int i = 0; i < n; i++) {
//        if (arr[i] > max1) {
//            max1 = arr[i];
//            maxIndex= i;
//        }
//        if (arr[i] < min1) {
//            min1 = arr[i];
//        }
//    }
//
//    double sum1 = 0;
//    for (int i = 0; i < n; i++) {
//        if ((arr[i]!=max1) && (arr[i]!=min1)) {
//            sum1 += arr[i];
//        }
//    }
//    double avg1 = sum1 / (n-2);
//
//    double absMax1 = 0;
//    for (int i = 0; i < n; i++) {
//        if ((arr[i]!=max1) && (arr[i]!=min1)) {
//            if (absMax1 < abs(arr[i])) {
//                absMax1 = abs(arr[i]);
//            }
//        }
//    }
//
//    cout << avg1  << " " << absMax1-avg1 << endl;
    int cnt = 0;
    while (1){

        // 找最大找最小，相减 a[maxIndex]-a[minIndex];
        cnt++;
        // 当我们遍历完一次之后，最大值仍然为0的时候,就代表可以结束了
        break;
    }


    return 0;
}