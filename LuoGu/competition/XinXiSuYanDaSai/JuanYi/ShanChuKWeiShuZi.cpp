#include <iostream>
#include <string>
using namespace std;

int main() {
    string num;
    int k;

    // 读取输入
    cin >> num;
    cin >> k;

    // 如果要删除的位数等于数字串长度，直接输出0
    if (k >= num.length()) {
        cout << "0" << endl;
        return 0;
    }

    // 使用贪心算法删除k个数字
    while (k > 0) {
        int i = 0;
        // 找到第一个递减的位置
        while (i < num.length() - 1 && num[i] <= num[i + 1]) {
            i++;
        }
        // 删除这个位置的数字
        num.erase(i, 1);
        k--;
    }

    // 处理前导零
    int leadingZeros = 0;
    while (leadingZeros < num.length() && num[leadingZeros] == '0') {
        leadingZeros++;
    }

    // 如果全是0，输出0，否则输出去掉前导零的结果
    if (leadingZeros == num.length()) {
        cout << "0" << endl;
    } else {
        cout << num.substr(leadingZeros) << endl;
    }

    return 0;
}


/*
 * 题目要求的策略（贪心删除）
题目要求我们删除 k 个数字，使得剩下的数字组成的整数最小。这种策略是位置优先的：
我们从左到右遍历数字串
当发现一个数字比它右边的数字大时，删除这个数字会使结果变小
重复这个过程 k 次，每次删除当前能使结果最小的数字
这种方法关注的是数字的位置，而不是数字的大小。因为高位的数字对结果的影响更大，所以我们优先删除高位上较大的数字。
删除前 k 个最大的数的策略
这种策略是大小优先的：
不考虑数字的位置，只关注数字的大小
直接删除整个数字串中最大的 k 个数字
这种方法完全忽略了位置对结果的影响，可能会导致结果不是最小的。

 一个简单的例子说明区别
假设输入是：
plaintext
321
2
题目要求的方法：
首先删除 3（因为 3>2），得到 21
然后删除 2（因为 2>1），得到 1
最终结果是 1
删除前 k 个最大的数的方法：
删除最大的数字 3，得到 21
删除剩余数字中最大的 2，得到 1
最终结果也是 1
这个例子中两种方法结果相同，但这只是巧合。让我们看一个更有说服力的例子：
plaintext
1432219
3
题目要求的方法：
删除 4（因为 4>3），得到 132219
删除 3（因为 3>2），得到 12219
删除 2（因为第二个 2>1），得到 1219
最终结果是 1219
删除前 k 个最大的数的方法：
删除最大的数字 9，得到 143221
删除第二大的数字 4，得到 13221
删除第三大的数字 3，得到 1221
最终结果是 1221
可以看到，两种方法得到了不同的结果，题目要求的方法得到了更小的数 1219。
为什么题目方法更优？
题目方法更优的原因是：
高位优先：高位上的数字对结果影响更大，所以优先删除高位上的大数
局部最优：每次删除都保证当前步骤得到的结果是最优的
整体最优：通过一系列局部最优决策，最终得到全局最优解
而直接删除前 k 个最大的数，可能会删除低位上的大数，导致高位上保留了较大的数，最终结果不是最小的。
总结
两种方法的核心区别在于：
题目方法：位置优先，贪心策略，每次删除当前能使结果最小的数字
删除前 k 个最大的数：大小优先，不考虑位置，直接删除最大的 k 个数字
 *
 *
 * */