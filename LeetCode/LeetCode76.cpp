#include "iostream"
#include "map"
#include "algorithm"
#include "cmath"
#include "string"
#include "vector"
using namespace std;

// 做一个滑动窗口，如果当前窗口内不满足形成t的，则右边界向右边移动，直到能满足，此时计算min(当前子串长度)
// 将左边界向右边移动，查看是否仍然满足，直到不能满足的前一个长度，进行min(当前子串长度)
// 既然不满足了，右侧指针接着向右边移动，重复上述两步

int main(){



    return 0;
}


