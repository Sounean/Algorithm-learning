#include "iostream"
#include "cmath"
using namespace std;

int main(){
    // 1.根据B的坐标去求出需要创建出来的二维数组map[22][22]

    // 2.将map数组的所有值先赋值成-1，马的落脚点的位子赋值成0，两条边界赋值成1(碰到0则跳过赋值)

    // 3.从左上角一直找到右下角，去相加可能的路线数量

    return 0;
}
